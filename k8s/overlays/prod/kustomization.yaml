apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../

namespace: realtime-prod

namePrefix: prod-

patchesStrategicMerge:
  - deployment-patch.yaml
  - service-patch.yaml
