apiVersion: v1
kind: Service
metadata:
  name: realtime-api-service
  labels:
    app: realtime-api
spec:
  type: LoadBalancer
  selector:
    app: realtime-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
